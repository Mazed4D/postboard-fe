const DEFAULT_STATE = {
	name: 'UNAUTHENTICATED',
	userId: 'UNAUTHENTICATED',
	JWT: 'UNAUTHENTICATED',
};

function storeReducer(state = DEFAULT_STATE, action) {
	switch (action.type) {
		case 'LOGIN':
	}
}
